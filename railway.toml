[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# BookStack exposes a simple health endpoint at /status.
healthcheckPath = "/status"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
